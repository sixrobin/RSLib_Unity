- /!\ Rendre indépendants les petits scripts de la library entre eux.

- AutoDestroy, avec delay, pour anim event.
- Textures de gradients linéaires/radiaux déjà utilisables, BW et WB.
- Sound Manager.
	- AudioOptions template avec les sliders nécessaires.
	- Trouver un moyen de faire loop un son et de le garder en ref pour le stopper. Renvoyer l'AudioSource ? Et faire gaffe à ne pas l'écraser avec le GetNextSource tout ça
	- Trouver un moyen de spatialiser.
	- PlayUISound à attacher sur les boutons/selectables.
	- PlaySounds pour jouer un Enumerable de sons.
	- ClipProvider.PlayToAudioManager ?
	- AudioManager delay dans PlaySound
- Image Effects : invert.
- Canvas Fade Manager : fade uni basique avec toute la custo (couleur, ordre, callback, etc.)
- Checker sur LD48 ce qu'il fallait améliorer/récupérer dans RSLib.
- Maze generator https://catlikecoding.com/unity/tutorials/maze/#a-get-key-down
- Pour les méthodes qui utilisent du random, créer des variantes qui prennent un System.Random en paramètre.
- RandomManager façon TLS.
- Pour les méthodes qui prennent en param des values type, créer les variantes avec le ref pour en faire des ref type.
	- Exemple: passer de x = x.Clamp() à x.Clamp() tout court.
- Récupérer le shader UIRamp de Templar.
	- Noter quelque part, dans le shader peut-être, les soucis avec les Graphic qui sont masqués.
- Maths : normalize from vector to vector
- Maths : stick to world direction (https://answers.unity.com/questions/31744/how-do-i-snap-vector-to-the-nearest-cardinal-value.html)
- Tilemap Utilities : RemoveCollision()
- Tilemap Utilities : méthode pour carve une tilemap selon une autre (murs secret de LDJAM50)
- Curve editor de Sebastian Lague
- World LayoutGroup (avec SpriteRenderers)
- Vector.Snap (avec en param le nombre de directions, 4, 8, etc.)
- Récupérer KeyCodeShortNames de Templar mais le changer en KeyCodeSymbols et ne garder que les symboles donc (et changer la méthode Get pour un TryGet? ou les deux ?)
- Checker les singleton et passer les properties static en membres.
- Singleton : méthodes log statiques.
- Homogénéiser les newlines des summaries.
- SprintBlink : override Color
- Settings (prévoir de quoi hériter pour ajouter des settings propres à chaque jeu)
	- Menu de base déjà tout fait et fonctionnel.
	- Bouton reset default.
- SpriteRandomizer de LDJAM50
- Fonction QuitApplicationPlatformDependent
- Color flash générique.

- Art
	- Tileset template de Templar
	- Cursors de Juice Jam
	- Coin de Templar
	- Puffs de Templar
	- Dither fade de Juice Jam
	- Tilesets + rule tiles de Juice Jam
	- Particle systems génériques (sang, footstep, hit, etc.)

- DungeonGenerator :
	- Drunkard Walk
	- Corridor
	- Binary Space Partitioning